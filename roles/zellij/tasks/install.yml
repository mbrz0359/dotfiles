- name: Install zellij
  ansible.builtin.copy:
    src: "zellij.toml"
    dest: "{{ ansible_facts['user_dir'] }}/.config/mise/conf.d/"
  notify: mise upgrade

- name: Copy zellij configuration
  ansible.builtin.copy:
    src: "config.kdl"
    dest: "{{ ansible_facts['user_dir'] }}/.config/zellij/"
