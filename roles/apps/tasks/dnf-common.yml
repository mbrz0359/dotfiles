- name: Remove unneeded dnf packages
  ansible.builtin.dnf:
    name:
      - evince
      - gnome-calculator
      - gnome-calendar
      - gnome-clocks
      - gnome-contacts
      - gnome-font-viewer
      - gnome-text-editor
      - libreoffice-core
      - snapshot
      - loupe
      - rhythmbox
      - mediawriter
    state: absent
  become: true
