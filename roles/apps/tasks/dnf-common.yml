- name: Remove unneeded dnf packages
  ansible.builtin.dnf:
    name:
      - evince
      - firefox
      - gnome-calculator
      - gnome-calendar
      - gnome-clocks
      - gnome-contacts
      - gnome-font-viewer
      - gnome-system-monitor
      - gnome-text-editor
      - libreoffice-core
      - loupe
      - mediawriter
      - rhythmbox
      - snapshot
    state: absent
  become: true
